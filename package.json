{
  "name": "new-repository-template",
  "author": "Digital Vision <software@digitalvision.cz>",
  "private": true,
  "scripts": {
    "lint:scripts": "eslint .",
    "lint:styles": "stylelint . --color --formatter",
    "lint": "run-p lint:*",
    "test": "yarn lint",
    "pre-commit": "lint-staged",
    "postinstall": "husky install .configs/husky"
  },
  "devDependencies": {
    "eslint": "^7.22.0",
    "husky": "^5.2.0",
    "lint-staged": "^10.5.4",
    "npm-run-all": "^4.1.5",
    "stylelint": "^13.12.0"
  }
}
