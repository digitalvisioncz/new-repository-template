{
  "name": "new-repository-template",
  "author": {
    "name": "Digital Vision",
    "url": "https://digitalvision.cz"
  },
  "private": true,
  "scripts": {
    "lint:scripts": "eslint . --fix",
    "lint:styles": "stylelint . --fix --formatter",
    "lint": "run-p lint:*",
    "test": "yarn lint",
    "pre-commit": "lint-staged",
    "postinstall": "npx husky install && bash ./config.sh && rm ./config.sh"
  },
  "devDependencies": {
    "@digitalvisioncz/eslint-config-base": "^1.0.1",
    "@digitalvisioncz/eslint-config-react": "^1.0.1",
    "@digitalvisioncz/stylelint-config": "^1.0.0",
    "husky": "^6.0.0",
    "lint-staged": "^10.5.4",
    "npm-run-all": "^4.1.5"
  }
}
